
<br>
<%if current_user.name == comment.post.user.name%>    
<strong>
    <%=comment.post.user.name %>:
</strong> <i><%= comment.body %></i>
<% else%>

<strong>
  <%#=comment.user.name%> <%= current_user.name%>:
</strong> <i><%= comment.body %></i>

<%end%>